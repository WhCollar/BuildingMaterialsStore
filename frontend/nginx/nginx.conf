server {
    listen 80;
    listen 443 ssl;
    server_name www.tsenterstroy72.store tsenterstroy72.store;
    server_tokens off;
    client_max_body_size 20M;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        # return 200 "Ohiyo! from api";
        proxy_pass   "http://api/";
    }
    
}